# TODO
- [x] Update frontend Signup to call backend `/api/auth/register` with correct workerDetails payload + show errors
- [x] Add OTP verification step in Signup calling `/api/auth/verify-otp` and store token
- [x] Update frontend Login to call backend `/api/auth/login` and store token
- [x] Smoke test signup/login flows

