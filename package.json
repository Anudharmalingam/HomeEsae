{
  "name": "home-maid",
  "version": "1.0.0",
  "description": "Home Maid Application",
  "scripts": {
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
